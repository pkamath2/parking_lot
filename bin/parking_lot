# Add script to run program here.
# Complete bin/setup so that after it is
# run, bin/parking_lot can be used to launch
# it.
BASEDIR=$(dirname "$0")
java -cp $BASEDIR/../target/parking_lot-1.0-SNAPSHOT.jar org.go_jek.solution.command.ParkingLotCLI $1